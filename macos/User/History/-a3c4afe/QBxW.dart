import 'package:flutter/material.dart';

class LinkButton extends StatelessWidget {
  const LinkButton({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('LinkButton');
  }
}
