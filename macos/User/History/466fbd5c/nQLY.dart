class OptionSelectFormField<T> extends StatelessWidget {
  const OptionSelectFormField({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('EnumSelectFormField');
  }
}
