part of '../observation_card.dart';

class _Identity extends StatelessWidget {
  const _Identity(this.data);

  final ObservationCardIdentityData data;

  @override
  Widget build(BuildContext context) {
    return const Text('_Identity');
  }
}
