import 'package:flutter/material.dart';

class ProfilePageShell extends StatelessWidget {
  const ProfilePageShell({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('ProfilePageShell');
  }
}
