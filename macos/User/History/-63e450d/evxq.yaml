api: MyApi
authority: "http://myapi.com"
http_client: http|dio
models: vanilla|freezed
default:
  protocol: http|https

endpoints:
  login:
    method: post
    response:
      success:
        token: String
      invalidCredentials:
        message: String