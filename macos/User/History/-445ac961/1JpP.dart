part of '../observation_card.dart';

class _Symptoms extends StatelessWidget {
  const _Symptoms(this.data);

  final ObservationCardSymptomsData data;

  @override
  Widget build(BuildContext context) {
    return const Text('_Symptoms');
  }
}
