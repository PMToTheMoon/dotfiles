import 'package:flutter/material.dart';
import 'package:neat/neat.dart';

class CandidateCompetencesView extends StatelessWidget {
  const CandidateCompetencesView({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        context.headline1(':)'),
      ],
    );
  }
}
