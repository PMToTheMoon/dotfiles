generators:
  test_generator:
    import: "package:test_generator/test_generator.dart"
    generator_factories:
      - testGenerator
    generator_extensions: {".dart": [".dart"]}
    defaults:
      hide_outputs: true
      consume_inputs: true
      generate_for: ["**.dart"]
      options:
        test: true