name: bonemeal_mirror
description: An experimental project to use mirror like feature at runtime
version: 1.0.0

environment:
  sdk: '>=2.17.6 <3.0.0'

dependencies:
  source_gen: ^1.2.2
  analyzer: ^4.4.0
  build: ^2.3.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.16.0
