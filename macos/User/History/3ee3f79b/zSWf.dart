import 'package:flutter/material.dart';

class ColoredShip extends StatelessWidget {
  const ColoredShip({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return InputChip(label: Text("Test"));
  }
}
