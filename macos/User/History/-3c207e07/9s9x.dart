import 'package:flutter/material.dart';

class SelectRecordPage extends StatelessWidget {
  const SelectRecordPage({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('SelectRecordPage');
  }
}
