api: ClientName

baseUrl: "http://test.onmyride.com"

endpoints:
  login:
    route: "/login"
    method: POST
    params:
    body:
      type: json|formurl-encoded|...
      inputs:
        email: String
        password: String
    outputs:
      success:
        status: String