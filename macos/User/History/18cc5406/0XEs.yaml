api: ClientName

baseUrl: "http://test.onmyride.com"

endpoints:
  login:
    route: "/login"
    method: POST
    params:
    body:
      type: json|formurl-encoded|...
      data:
        email: String
        password: String
    outputs:
      success:
        status: String