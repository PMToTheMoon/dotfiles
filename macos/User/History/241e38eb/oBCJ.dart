part of '../observation_card.dart';

class _MedicalHistory extends StatelessWidget {
  const _MedicalHistory({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('_MedicalHistory');
  }
}
