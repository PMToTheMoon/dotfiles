part of '../observation_card.dart';

class _Treatments extends StatelessWidget {
  const _Treatments({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('_Treatments');
  }
}
