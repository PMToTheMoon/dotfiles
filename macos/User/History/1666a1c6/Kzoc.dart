import 'package:flutter/material.dart';

class ObservationCard extends StatelessWidget {
  const ObservationCard({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('ObservationCard');
  }
}
