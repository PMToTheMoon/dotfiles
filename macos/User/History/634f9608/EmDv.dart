import 'package:flutter/material.dart';

class CandidatePageShell extends StatelessWidget {
  const CandidatePageShell({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('CandidatePageShell');
  }
}
