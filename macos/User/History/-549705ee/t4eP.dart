part of '../observation_card.dart';

class _HiddenObservation extends StatelessWidget {
  const _HiddenObservation({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text('_HiddenObservation');
  }
}
