name: bonemeal
description: Grows seeds.
version: 1.0.0

environment:
  sdk: '>=2.16.2 <3.0.0'

dependencies:
  completion: ^1.0.0
  build_cli_annotations: ^1.0.0

dev_dependencies:
  lints: ^1.0.0
  build_cli: ^1.0.0
  build_runner: ^1.0.0
