part of 'simple_login_sm.dart';

class UserRepository {
  void login({
    required String email,
    required String password,
  }) {
    //login using api
    //return void or throw an error if login fail
  }
}
